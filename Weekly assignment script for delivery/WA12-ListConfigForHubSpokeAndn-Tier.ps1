# Variables
$prefix = 'demo'
$resourceGroupName = $prefix + '-rg-network-001'
$resourceGroupNameVM = $prefix + '-rg-vm-001'
$location = 'uksouth'

# Get All VNETs and Subnets that starts with the $prefix
